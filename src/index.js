import React from 'react';
import ReactDOM from 'react-dom';

const element = <h1>Hello, Badges from React!</h1>;

const container = document.getElementById('app');

ReactDOM.render(element, container);
